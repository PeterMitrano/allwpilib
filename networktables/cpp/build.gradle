apply plugin: 'cpp'

libraries {
    networkTables {}
}

sources {
    networkTables {
        cpp {
            source {
                srcDirs = ["lib/share", "lib/Athena"]
                includes = ["**/*.cpp"]
            }
            exportedHeaders {
                srcDirs = ["include"]
            }
        }
    }
}